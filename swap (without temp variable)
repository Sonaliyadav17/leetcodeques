class Solution{
    static List<Integer> get(int a,int b)
    {
        a = a + b;
        b = a - b;
        a = a - b;
        
        List<Integer> swappedValues = new ArrayList<>();
        swappedValues.add(a);
        swappedValues.add(b);
        
        return swappedValues;
    }
}
